server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # React SPA — serve index.html for any unknown route
    location / {
        try_files $uri /index.html;
    }

    # SSE streaming endpoint — disable proxy buffering so tokens reach the client immediately
    location /api/defects/query/stream {
        proxy_pass         http://api:8080/defects/query/stream;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_read_timeout 300s;
        proxy_buffering    off;
        proxy_cache        off;
    }

    # Proxy /api/* → api service (strips the /api prefix)
    location /api/ {
        proxy_pass         http://api:8080/;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_read_timeout 120s;
    }
}
